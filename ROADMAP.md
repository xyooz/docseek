# DocSeek Roadmap

DocSeek 的目标是做成一个适合 Windows 办公环境长期常驻使用的本地全文检索工具，而不是持续堆功能的 Demo。

路线遵循一个原则：**先把传统全文检索做快、做准、做稳，再考虑可选的智能检索。**

## 当前阶段：v0.x — 核心检索可用

已经具备：

- 多目录本地索引与排除目录；
- Word / Excel / PowerPoint / PDF / 文本内容解析；
- Chunk 级索引与精确命中位置；
- SQLite FTS5 + unicode61 / CJK bigram；
- BM25 + 文件名命中加权；
- 文件级精确分页；
- `ext:` / `path:` / `after:` / `before:` / `size:` 筛选；
- `page:` / `slide:` / `sheet:` 结构定位提示；
- Excel 工作表名、PPT/Word 标题等轻量结构排序信号；
- 无关键词纯筛选浏览；
- watchdog 精确单文件增量更新；
- 索引失败持久记录与查看；
- Windows CI、compile、桌面模块 smoke import；
- Schema Version 与显式迁移；
- Extractor Revision 渐进式重建；
- 可重复性能基准与 10k / 50k 规模档；
- Recall@K / MRR / nDCG 检索质量回归门槛；
- 搜索总命中文件数与搜索耗时显示；
- 相关性 / 最近修改 / 文件名排序；
- 可点击筛选 Chips、键盘导航与可操作空状态；
- `QTableView + QAbstractTableModel` 增量结果列表。

这一阶段的目标不是“功能最多”，而是验证：**普通办公人员是否真的能比 Everything 的文件名搜索更快找到记得内容、不记得标题的文档。**

---

## Phase 1：v0.3 — 稳定性与规模化基础

核心机制已基本建立，后续重点从“有没有”转向“真实环境验证”。

### 1. 数据库迁移与渐进式重建

当前已实现：

```text
旧 Schema
   ↓ 显式 migrate
新 Schema

旧 Extractor Revision
   ↓ 仅重建受影响格式
新 Extractor Revision
```

现有约束：

- 启动时识别数据库版本；
- 新版本数据库不会被旧版本逻辑静默改写；
- Schema 迁移尽量不重新解析源文档；
- 解析器升级通过按格式 revision 渐进补索引；
- 目前 DOCX / PPTX 的结构增强可以只刷新对应旧索引，不牵连其他格式；
- 重建成功后 revision 与正文写入处于同一事务，避免半完成状态。

后续仍需要补：

- 迁移失败后的用户级恢复提示；
- 大版本升级前的索引备份 / 回滚策略；
- 更多历史 Schema 跨版本升级组合测试。

### 2. 规模性能基线

固定测试档位：

- 1k 文件：每次 CI 快速回归；
- 10k 文件：可触发的普通办公规模基线；
- 50k 文件：可触发的重点压力门槛；
- 100k 文件：后续压力观察，不作为当前硬门槛。

当前 Windows CI 合成 mixed-office workload 已验证：

- 10k 文件：`ExactGroupedSearchEngine` 目标门槛 P95 < 200 ms，通过；
- 50k 文件：目标门槛 P95 < 500 ms，通过；
- 50k / 150k Chunk 测试中，常见高命中查询 P95 约 182 ms；
- 50k 测试逻辑正文约 585.9 MiB，索引数据库约 249.4 MiB，约 0.43×；
- 12 类 common / medium / rare / filename / multi-term / phrase / metadata-filter workload 均进行 Exact 结果一致性检查；
- 单文件更新与 unchanged 扫描已有独立 benchmark。

这些结果说明当前架构在 **CI 合成负载** 下已经达到既定 10k / 50k 查询目标，但不能替代真实办公环境测试。仍需持续记录并验证：

- 真实 Windows 办公机 10k～50k 文件；
- 真实 DOCX / XLSX / PPTX / PDF 解析成本，而不仅是合成 Chunk；
- 2 字中文短词与高频业务词；
- 内存峰值；
- 大 Excel / PDF 首次解析；
- 网络盘 / 同步盘场景；
- 长时间运行后的数据库与 watcher 状态。

性能优化规则：**正确性优先于单次 benchmark。** 已验证过 metadata prefilter 等实验路径；如果收益不能跨 Windows runner 稳定复现，则不进入生产搜索主链路。

### 3. 搜索正确性

现有回归已覆盖文件级分页、深分页、结构提示、元数据过滤、中文连续词、英文短语、文件名加权等主路径。

继续补边界：

- Windows 路径大小写与盘符规范化；
- 文件移动 / 重命名 / 覆盖保存；
- 网络盘、同步盘和临时文件事件；
- 只读文件、锁定文件、损坏文件；
- 中文标点、数字、英文混合关键词；
- 单汉字查询的策略与限制；
- 长时间 watcher soak test 与异常恢复。

---

## Phase 2：v0.4 — 产品体验

目标是从“开发者工具”转向“同事愿意每天开着用”。

### 搜索体验

已完成：

- 显示总命中文件数；
- 显示搜索耗时；
- 滚动增量加载；
- 类型筛选与高级语法；
- 更明确的空状态与首次使用引导；
- 可点击筛选 Chips，降低记忆 `ext:` / `path:` 等语法成本；
- 排序切换：相关性 / 最近修改 / 文件名；
- 快捷键上下选择、Enter 打开、Esc 清空。

下一步：

- 搜索历史；
- 常用筛选保存；
- 搜索帮助弹层，而不是要求用户阅读 README。

### 结果列表

已完成：

- 从 `QTableWidget` 迁移到 `QTableView + QAbstractTableModel`；
- 每行直接持有一个 `ChunkSearchResult`，避免为每列创建独立 Item；
- 分页结果通过 `beginInsertRows / endInsertRows` 增量追加；
- 选中、预览、打开、定位、复制路径统一从 Model 读取；
- 使用 offscreen `MainWindow` smoke test 覆盖真实 View / Model 接线。

下一步：

- 保存列宽与列显示状态；
- 评估文件类型图标和轻量状态标记；
- 优化大量结果持续追加时的 UI 内存和绘制基线。

### 索引管理

- 单个索引目录暂停 / 恢复；
- 排除文件名、扩展名和通配规则；
- 索引大小和最后更新时间；
- 一键重建索引；
- 一键清理索引；
- 异常文件统计与重试。

---

## Phase 3：v0.5 — 检索质量专项

目标是让“搜得到”逐步变成“前几条就是我要的”。

### 已建立的质量门槛

当前 CI 已有 curated query set，并计算：

- Recall@K；
- MRR；
- nDCG@K。

当前小型 curated 集上的 Recall@5 / MRR / nDCG@5 均为 1.0。这个数字只表示**标准回归样例没有退化**，不能解释为真实办公搜索准确率 100%。

后续最重要的是逐步加入脱敏的真实办公查询案例，使离线质量集更接近实际使用。

### 排序信号

当前已经在使用：

```text
文件名精确命中
+ 文件名前缀 / 包含
+ 正文 BM25
+ 显式 page / slide / sheet 意图
+ Excel 工作表名 / 标题等轻量结构信号
```

后续候选：

```text
同文件多个 Chunk 命中次数（封顶）
+ 更可靠的 heading / title 级别
+ 很小权重的最近修改时间
```

每个信号都必须先过质量集与延迟基准，不做拍脑袋加权。

### 中文检索

对比：

- 当前 CJK bigram；
- jieba 等轻量本地分词；
- 领域词典；
- 拼音首字母；
- 错别字 / 近似搜索。

优先保证银行办公语料常见词，例如制度名称、业务术语、产品名称、岗位名称和流程词。

---

## Phase 4：v0.6 — 安全与部署

正式在办公环境推广前必须完成。

### 本地数据安全

当前 `docseek.db` 会保存提取后的正文 Chunk，因此需要按可能包含敏感内容的数据文件处理。

需要评估：

- 数据库目录 Windows ACL；
- 索引数据库加密的成本与收益；
- 是否支持“仅索引 Token、不保存完整正文”的安全模式；
- 删除源文件后的索引清理时效；
- 临时文件和日志是否残留正文；
- 索引备份策略；
- 用户退出 / 卸载后的数据清理。

### Windows 发布

稳定后再进入：

- PyInstaller / Nuitka 对比；
- 单文件或目录式安装包；
- 无管理员权限运行；
- 自动启动可选；
- 应用图标、版本信息；
- 升级兼容；
- 内网无互联网环境安装验证。

不要在底层仍快速变化时过早投入安装包维护。

---

## Phase 5：v1.0 — 可推广的本地办公检索工具

v1.0 的判断标准不是“代码写完”，而是：

1. 在真实 Windows 办公机持续运行稳定；
2. 真实办公目录 10k～50k 文件性能可接受；
3. 文件新增、保存、删除、移动后索引状态可靠；
4. 普通用户无需学习语法也能完成大部分检索；
5. 索引异常有明确反馈和恢复手段；
6. 数据安全边界经过内部环境评估；
7. 安装、升级、卸载路径清楚；
8. 有一批真实检索案例证明结果质量优于只搜文件名。

---

## v1.x 之后：可选 Hybrid Search

只有传统全文检索已经稳定后，再评估本地语义检索：

```text
关键词 FTS5 / BM25 召回
              +
Chunk 级本地 Embedding 召回
              ↓
            重排序
```

约束：

- 完全本地可选；
- 不替换关键词检索；
- 不把大模型作为基本运行依赖；
- CPU 办公机也必须能关闭智能功能后流畅使用；
- 需要真实证明语义召回能解决“记得意思但记不得原词”的场景。

可能的典型场景：用户记得“身份证过期怎么更新”，但文件原文标题是“个人客户证件有效期维护操作指引”。

---

## 暂不做的事情

当前明确不优先：

- Elasticsearch / OpenSearch；
- 云端全文索引；
- 强依赖 GPU；
- 在线 LLM；
- 多用户服务器模式；
- 大而全的知识库平台；
- 为了技术新颖而加入向量数据库。

DocSeek 当前最有价值的产品定位仍然是：

> **在一台普通 Windows 办公电脑上，像 Everything 一样简单地搜索本地 Office/PDF 文档的正文。**
