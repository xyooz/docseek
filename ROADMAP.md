# DocSeek Roadmap

DocSeek 的目标是成为一个适合 Windows 办公环境长期常驻使用的本地全文检索工具，而不是持续堆功能的 Demo。

路线原则：**先把传统全文检索做快、做准、做稳，再考虑可选智能检索；进入 Beta 后，真实使用证据优先于继续增加功能。**

## 当前阶段：0.2.0 Beta — 核心能力完成，转向真实环境验证

目前已经具备完整的“源码 → 测试 → Windows 交付候选”链路：

- 多目录本地索引、排除目录、文件通配排除；
- 单目录暂停 / 恢复；
- DOCX / XLSX / PPTX / PDF / 文本专用解析；
- Calamine / native Tika / WPS 可选兼容路径；
- 结构化 DocIR 与精确命中位置；
- SQLite FTS5 + unicode61 / CJK bigram；
- ExactGroupedSearchEngine + BM25 + 文件名 / 结构信号；
- 文件级精确分页；
- 元数据筛选与 `page:` / `slide:` / `sheet:`；
- watchdog 精确增量更新；
- 索引问题持久化、重试和健康摘要；
- Schema v8 + 显式迁移；
- Extractor Revision 渐进式重建；
- Recall@K / MRR / nDCG 回归集；
- 1k 常规 CI 与 10k / 50k / 更大规模 benchmark 档；
- QTableView 结果模型、历史、收藏、筛选 Chips、搜索帮助；
- Windows Portable ZIP；
- 无管理员权限的每用户 Setup.exe；
- 安装 → frozen smoke → 卸载 → 用户数据保留的 CI 生命周期验证；
- guarded GitHub Release pipeline 与 SHA256SUMS；
- 脱敏 Beta 诊断导出，不包含正文、文件名、路径和问题详情。

当前阶段不把“CI 全绿”解释为“已经可以大范围企业部署”。下一步需要真实办公机证据。

---

## P0：真实 Windows Beta 验证

这是当前最高优先级。

详细方法见 [`docs/BETA_TESTING.md`](docs/BETA_TESTING.md)。

### 1. 安装与启动

验证：

- 普通用户安装，无 UAC 管理员提权；
- `%LOCALAPPDATA%\Programs\DocSeek` 每用户安装；
- Portable 解压即用；
- 无 Python 环境机器可正常启动；
- 卸载只删除程序，不误删 `%USERPROFILE%\.docseek` 用户状态；
- 内网无互联网环境安装与首次运行。

### 2. 真实文件规模

建议分阶段：

- 1k～10k：快速验证格式和工作流；
- 10k～50k：主要办公规模；
- 50k+：压力观察，不先设为普通用户硬门槛。

记录：首次索引时间、格式分布、数据库大小、问题数、CPU/内存峰值、大 Excel/PDF 长尾。

### 3. 真实检索质量

逐步建立脱敏真实查询集，而不是只使用合成样例。

关注：

- 已知目标文件是否进入 Top1 / Top5 / Top10；
- 普通正文词、制度名、业务术语；
- Word 标题、PPT 标题、Excel 工作表；
- 中文 / 英文 / 数字混合；
- 元数据筛选组合；
- 用户是否需要频繁改写查询。

CI curated 集继续作为“防退化门槛”，不能被表述为真实准确率 100%。

### 4. Watcher 与长期运行

必须补真实环境 soak：

- 创建 / 修改 / 重命名 / 删除 / 移动；
- Office 覆盖保存和临时文件；
- 子目录整体变化；
- 睡眠 / 唤醒；
- 4～8 小时持续运行；
- 1～3 天日常使用；
- 网络盘 / 同步盘断开恢复（仅在实际需要时）。

观察内存是否持续增长、CPU 空闲占用、WAL 状态、watcher 是否漏事件、关闭是否干净。

---

## P1：维护与恢复能力

目标：索引不是“一次性缓存”，而是可以长期维护的本地数据资产。

已经完成：

- Schema Version 与显式迁移；
- 新版本数据库拒绝被旧逻辑静默修改；
- Extractor Revision 按格式刷新；
- 索引健康摘要；
- 最近完整校准时间；
- 索引问题持久记录和安全重试；
- 活动 / 暂停根目录状态。

下一步：

1. **一键重建索引**：明确哪些配置保留、哪些数据重建；
2. **一键清理索引**：必须清晰展示只删除 DocSeek 索引，不删除源文件；
3. **升级前备份 / 回滚**：尤其针对未来大版本 schema 迁移；
4. **数据库健康检查**：必要时增加 integrity check / recovery 提示；
5. **诊断增强**：继续保持默认脱敏，避免为了可观测性导出办公路径和正文。

---

## P2：检索质量专项

目标：从“搜得到”进一步变成“前几条就是我要的”。

当前排序信号：

```text
文件名精确命中
+ 文件名前缀 / 包含
+ 正文 BM25
+ 显式 page / slide / sheet 意图
+ 工作表名 / 标题等轻量结构信号
```

候选信号必须先通过真实质量集和延迟基准：

- 同文件多个 Chunk 命中次数（封顶）；
- 更可靠的 heading/title 层级；
- 很小权重的最近修改时间；
- 领域词典；
- 拼音首字母；
- 错别字 / 近似搜索。

### 中文检索原则

当前 CJK bigram 已经是稳定生产基线。jieba、trigram、紧凑候选索引等方案可以继续实验，但只有在**真实质量 + 延迟 + 体积 + 迁移风险**综合更优时才替换。

Progressive Top-K 曾在密集查询上更快，但结果召回不足，因此继续保留实验性质，不进入用户搜索主链路。

---

## P3：Windows 发布成熟度

### 已完成

- PyInstaller onedir Portable；
- Portable 构建后 frozen smoke；
- 最终 ZIP 干净解压后再次 smoke；
- Inno Setup 每用户安装；
- `PrivilegesRequired=lowest`，不要求管理员权限；
- 开始菜单入口、可选桌面快捷方式；
- Setup 安装 / 运行 / 卸载生命周期 CI；
- 卸载保留用户索引和设置；
- `docseek.__version__` 作为发布版本唯一来源；
- tag / 版本一致性硬校验；
- Portable + Setup + SHA256SUMS release candidate；
- PR / manual dry-run 不会误创建正式 Release；
- `v*` tag 才允许发布 GitHub Release。

### 下一步

1. **代码签名**：解决 Windows SmartScreen / 企业终端信任问题；
2. **应用图标和版本资源**：完善 Windows 文件属性和品牌识别；
3. **升级体验**：验证旧版本覆盖安装、用户设置/索引兼容；
4. **企业软件分发**：评估内部软件中心、静默部署参数；
5. **中文安装向导**：只引入许可证明确、版本受控的翻译资源，不在 CI 临时下载未知第三方文件；
6. **自动启动**：如果真实用户确有需求，再做成明确可选项，而不是默认常驻。

当前不急于创建“正式 GA”标签。发布流水线已经具备，不代表产品已经完成真实办公环境准入。

---

## P4：本地数据安全

`docseek.db` 保存提取后的正文 Chunk，因此安全目标不能只看“应用是否联网”。

需要进一步评估：

- 数据库目录 Windows ACL；
- 是否需要数据库级加密；
- 是否值得提供“减少/不保存完整原文”的安全索引模式；
- 删除源文件后的清理时效；
- WAL、临时文件、日志是否残留正文；
- 索引备份的敏感性；
- 用户离岗 / 设备移交 / 卸载后的清理流程；
- 网络盘 / 共享目录原 ACL 与本地索引副本之间的权限语义。

已经完成的脱敏诊断是这一方向的一部分：故障报告默认不导出根目录、文件名、路径、正文、问题详情或搜索记录。

---

## P5：v1.0 准入标准

v1.0 不是“代码写完”，而是至少满足：

1. 真实 Windows 办公机持续运行稳定；
2. 真实 10k～50k 文件性能可接受；
3. 创建、保存、删除、移动后的索引最终一致；
4. 睡眠 / 唤醒和常见同步盘场景可恢复；
5. 普通用户无需学习复杂语法即可完成大部分检索；
6. 真实查询集证明 Top5 质量有实际价值；
7. 失败文件不会拖垮整个索引任务；
8. 索引异常有明确反馈、重试和恢复路径；
9. 数据安全边界经过目标办公环境评估；
10. 安装、升级、卸载、备份、清理路径清楚；
11. 代码签名 / 企业分发策略达到实际推广要求；
12. 有足够 Beta 反馈证明用户愿意长期保留并使用 DocSeek。

---

## v1.x 以后：可选 Hybrid Search

只有传统全文检索在真实环境中稳定后，再评估：

```text
FTS5 / BM25 关键词召回
              +
Chunk 级本地 Embedding 召回
              ↓
            重排序
```

约束：

- 完全本地、可关闭；
- 不替换关键词检索；
- 不把大模型作为基本运行依赖；
- 普通 CPU 办公机关闭智能功能后仍必须流畅；
- 必须真实证明它能解决“记得意思但记不得原词”的问题。

例如：用户记得“身份证过期怎么更新”，原文可能写的是“个人客户证件有效期维护操作指引”。只有这类传统关键词难以解决的真实问题足够多时，Hybrid Search 才值得增加复杂度。

---

## 暂不优先

当前明确不优先：

- 云端文档上传或在线向量服务；
- 强依赖 GPU / 大模型才能搜索；
- 为追求 benchmark 单项更快而牺牲精确结果语义；
- 在没有真实用户需求前堆大量高级语法；
- 在签名、真实 Beta、数据安全尚未验证前宣称企业级正式可用。
