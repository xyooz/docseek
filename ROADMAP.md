# DocSeek Roadmap

DocSeek 的目标是做成一个适合 Windows 办公环境长期常驻使用的本地全文检索工具，而不是持续堆功能的 Demo。

路线遵循一个原则：**先把传统全文检索做快、做准、做稳，再考虑可选的智能检索。**

## 当前阶段：v0.x — 核心检索可用

已经具备：

- 多目录本地索引与排除目录；
- Word / Excel / PowerPoint / PDF / 文本内容解析；
- Chunk 级索引与精确命中位置；
- SQLite FTS5 + unicode61 / CJK bigram / trigram；
- BM25 + 文件名命中加权；
- 文件级分页；
- `ext:` / `path:` / `after:` / `before:` / `size:` 筛选；
- 无关键词纯筛选浏览；
- watchdog 精确单文件增量更新；
- 索引失败持久记录与查看；
- Windows CI、compile、桌面模块 smoke import；
- 数据库 Schema Version 基础；
- 可重复性能基准脚本。

这一阶段的目标不是“功能最多”，而是验证：**普通办公人员是否真的能比 Everything 的文件名搜索更快找到记得内容、不记得标题的文档。**

---

## Phase 1：v0.3 — 稳定性与规模化基础

优先级最高。

### 1. 数据库迁移机制

当前已经开始记录 Schema Version。下一步需要把未来的结构变更写成明确迁移步骤：

```text
v1
 ↓ migrate
v2
 ↓ migrate
v3
```

要求：

- 升级前识别数据库版本；
- 不允许旧程序写入新版本数据库；
- 可重建的全文索引与不可丢的用户设置分开处理；
- 迁移失败时保留旧索引或给出明确的“重建索引”路径。

### 2. 规模性能基线

固定测试档位：

- 1k 文件：开发期快速回归；
- 10k 文件：普通办公资料规模；
- 50k 文件：重点性能门槛；
- 100k 文件：压力观察，不作为当前硬门槛。

持续记录：

- 首次索引耗时；
- 增量单文件更新耗时；
- 数据库体积；
- 常用关键词查询 P50 / P95；
- 2 字中文短词查询；
- 3+ 字中文连续查询；
- 内存峰值；
- UI 首屏结果时间。

建议当前目标：

- 10k 文件常见查询首屏 < 200 ms；
- 50k 文件常见查询首屏尽量 < 500 ms；
- 单文件更新不触发全目录遍历；
- 大 Excel / PDF 不因全文拼接造成明显内存峰值。

这些数字是产品目标，不是现在已经验证达到的结论，必须由真实 Windows 办公机基准确认。

### 3. 搜索正确性

继续补边界：

- Windows 路径大小写与盘符规范化；
- 文件移动 / 重命名 / 覆盖保存；
- 网络盘、同步盘和临时文件事件；
- 只读文件、锁定文件、损坏文件；
- 中文标点、数字、英文混合关键词；
- 单汉字查询的策略与限制。

---

## Phase 2：v0.4 — 产品体验

目标是从“开发者工具”转向“同事愿意每天开着用”。

### 搜索体验

- 显示总命中文件数；
- 搜索耗时；
- 更明确的空状态与首次使用引导；
- 搜索历史；
- 常用筛选保存；
- 可点击筛选 Chips，降低记忆语法成本；
- 排序切换：相关性 / 最近修改 / 文件名；
- 快捷键上下选择、Enter 打开、Esc 清空；
- 搜索帮助弹层，而不是要求用户阅读 README。

### 结果列表

从 `QTableWidget` 迁移到 `QTableView + QAbstractTableModel`：

- 大结果集更低 UI 开销；
- 更自然的增量加载；
- 列宽、列显示状态可保存；
- 更方便加入图标、排序和状态标记。

### 索引管理

- 单个索引目录暂停 / 恢复；
- 排除文件名、扩展名和通配规则；
- 索引大小和最后更新时间；
- 一键重建索引；
- 一键清理索引；
- 异常文件统计与重试。

---

## Phase 3：v0.5 — 检索质量专项

目标是让“搜得到”逐步变成“前几条就是我要的”。

### 排序信号

在 BM25 基础上逐步验证：

```text
文件名精确命中
+ 文件名前缀 / 包含
+ 正文 BM25
+ 同文件多个 Chunk 命中次数（封顶）
+ 命中位置类型
+ 很小权重的最近修改时间
```

每个信号都需要真实案例和离线测试，不做拍脑袋加权。

### 中文检索

对比：

- 当前 bigram / trigram；
- jieba 等轻量本地分词；
- 领域词典；
- 拼音首字母；
- 错别字 / 近似搜索。

优先保证银行办公语料常见词，例如制度名称、业务术语、产品名称、岗位名称和流程词。

---

## Phase 4：v0.6 — 安全与部署

正式在办公环境推广前必须完成。

### 本地数据安全

当前 `docseek.db` 会保存提取后的正文 Chunk，因此需要按可能包含敏感内容的数据文件处理。

需要评估：

- 数据库目录 Windows ACL；
- 索引数据库加密的成本与收益；
- 是否支持“仅索引 Token、不保存完整正文”的安全模式；
- 删除源文件后的索引清理时效；
- 临时文件和日志是否残留正文；
- 索引备份策略；
- 用户退出 / 卸载后的数据清理。

### Windows 发布

稳定后再进入：

- PyInstaller / Nuitka 对比；
- 单文件或目录式安装包；
- 无管理员权限运行；
- 自动启动可选；
- 应用图标、版本信息；
- 升级兼容；
- 内网无互联网环境安装验证。

不要在底层仍快速变化时过早投入安装包维护。

---

## Phase 5：v1.0 — 可推广的本地办公检索工具

v1.0 的判断标准不是“代码写完”，而是：

1. 在真实 Windows 办公机持续运行稳定；
2. 真实办公目录 10k～50k 文件性能可接受；
3. 文件新增、保存、删除、移动后索引状态可靠；
4. 普通用户无需学习语法也能完成大部分检索；
5. 索引异常有明确反馈和恢复手段；
6. 数据安全边界经过内部环境评估；
7. 安装、升级、卸载路径清楚；
8. 有一批真实检索案例证明结果质量优于只搜文件名。

---

## v1.x 之后：可选 Hybrid Search

只有传统全文检索已经稳定后，再评估本地语义检索：

```text
关键词 FTS5 / BM25 召回
              +
Chunk 级本地 Embedding 召回
              ↓
            重排序
```

约束：

- 完全本地可选；
- 不替换关键词检索；
- 不把大模型作为基本运行依赖；
- CPU 办公机也必须能关闭智能功能后流畅使用；
- 需要真实证明语义召回能解决“记得意思但记不得原词”的场景。

可能的典型场景：用户记得“身份证过期怎么更新”，但文件原文标题是“个人客户证件有效期维护操作指引”。

---

## 暂不做的事情

当前明确不优先：

- Elasticsearch / OpenSearch；
- 云端全文索引；
- 强依赖 GPU；
- 在线 LLM；
- 多用户服务器模式；
- 大而全的知识库平台；
- 为了技术新颖而加入向量数据库。

DocSeek 当前最有价值的产品定位仍然是：

> **在一台普通 Windows 办公电脑上，像 Everything 一样简单地搜索本地 Office/PDF 文档的正文。**
