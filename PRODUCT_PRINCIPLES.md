# DocSeek Product Principles

DocSeek 的核心目标不是做一个“大而全”的知识库平台，而是做成一个普通 Windows 办公人员愿意长期留在电脑上的本地搜索工具。

> **DocSeek = Everything for document contents.**
>
> 用户记得文件里写过什么，但不记得文件名时，也能像用 Everything 一样快速找到并打开它。

## 核心使用链路

```text
打开 / 唤起 DocSeek
        ↓
输入记得的内容
        ↓
立刻看到可信结果
        ↓
↑ / ↓ 选择
        ↓
Enter 打开文件
```

任何新功能都应优先缩短、保护或增强这条链路，而不是让主界面越来越复杂。

## 功能准入规则

新增功能前先回答两个问题：

1. **它能不能让用户更快、更容易找到目标文件？**
2. **它能不能让 DocSeek 更稳定、更安全地长期运行？**

如果两个答案都不明显，就不进入近期主线。

## 当前优先级

1. 真实 Windows 办公环境下的稳定性与长时间运行验证；
2. 首次索引、增量更新、异常恢复和索引状态透明度；
3. 安装、升级、卸载等普通用户使用路径；
4. 基于真实脱敏查询集改进检索质量；
5. 只有在真实问题证明有必要时，再增加新的排序或智能检索能力。

## 产品边界

近期不把 DocSeek 扩张成：

- 云端知识库；
- 多用户搜索服务器；
- 在线大模型 / RAG 平台；
- 文件编辑器或文件管理器；
- 强依赖 GPU 的语义搜索产品；
- Elasticsearch / OpenSearch 运维项目。

未来的可选智能检索也必须建立在传统本地全文检索已经足够快、准、稳的基础上，并且不能破坏纯 CPU、离线、轻量运行这一基本能力。

## 产品判断标准

DocSeek 的成功不是“功能很多”，而是：

- 普通用户无需学习复杂语法也能找到文件；
- 打开后可以迅速完成搜索并进入原文档；
- 文件新增、保存、删除、移动后索引长期可靠；
- 索引异常可见、可解释、可恢复；
- 应用可以在普通 Windows 办公机长期常驻；
- 真实使用案例证明它能解决“记得内容、忘了标题”的问题。
