name: tests

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install
        run: python -m pip install -e .
      - name: Compile
        run: python -m compileall -q src tests benchmarks
      - name: Smoke import desktop app
        run: python -c "import docseek.app"
      - name: Benchmark smoke
        run: python benchmarks/benchmark_search.py --files 20 --chunks 2 --iterations 1 --limit 10
      - name: Directory scan smoke
        run: python benchmarks/benchmark_scan.py --files 20
      - name: Test
        run: python -m unittest discover -s tests -v
