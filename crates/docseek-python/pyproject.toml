[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "docseek-rust"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
bindings = "pyo3"
module-name = "docseek_rust"

